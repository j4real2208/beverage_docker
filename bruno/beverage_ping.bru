meta {
  name: Beverage Service - Ping
  type: http
  seq: 2
}

get {
  url: http://0.0.0.0:8080/ping
  body: none
  auth: inherit
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response should be 'pong'", function() {
    expect(res.getBody()).to.equal("pong");
  });
}
