meta {
  name: Management Service - Delete Beverage
  type: http
  seq: 4
}

delete {
  url: http://0.0.0.0:8090/management/beverages/{id}
  body: none
  auth: inherit
}

vars:path {
  id: 1
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
